pip cache purge  # 清除缓存

pip freeze > requirements.txt
pip install -r requirements.txt


python -m pip install python-dotenv loguru starlette uvicorn



uvicorn otter:app --host 0.0.0.0 --port 7788



python -m pip install hypercorn trio

hypercorn --worker-class trio app:app

